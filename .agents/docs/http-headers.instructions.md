---
applyTo: "apps/website/docs/public/_headers,apps/website/Caddyfile"
description: "Instructions for setting up HTTP headers for security and performance."
---

# HTTP headers

Keep `_headers` for Cloudflare and `Caddyfile` for Docker equivalent. Harden both configurations without breaking the application, and validate them with HTTP Observatory and Security Headers.
